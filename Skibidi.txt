function TP(Pos)
    local lp = game.Players.LocalPlayer
    local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
    local hum = lp.Character and lp.Character:FindFirstChild("Humanoid")
    if not hrp or not hum then return end

    if hum.Sit then hum.Sit = false end

    hrp.CFrame = Pos
end
    
local vu = game:GetService("VirtualUser")
_G.AntiAFK = true

if _G.AntiAFK then
repeat wait() until game:IsLoaded()

game:GetService("Players").LocalPlayer.Idled:Connect(function()
    game:GetService("VirtualUser"):ClickButton2(Vector2.new())
    vu:Button2Down(Vector2.new(0, 0), camera.CFrame)
    wait(1)
    vu:Button2Up(Vector2.new(0, 0), camera.CFrame)
end)
end

task.spawn(function()
    while true do
        pcall(function()
            vu:CaptureController()
            vu:Button1Down(Vector2.new(1280, 672))
        end)
        task.wait(10)
    end
end)

local Players = game:GetService("Players")
local player = game:GetService("Players").LocalPlayer
local lp = Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local backpack = lp:WaitForChild("Backpack")
local character = lp.Character or lp.CharacterAdded:Wait()
local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

local FlurioreFixLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/aoowkwjwj/Hello/refs/heads/main/XinChaoNheee.lua"))()
local GuiSettings = {
    ["NameHub"] = "Blue X Hub | Plant Vs Brainrot V1.0.5",
    ["Description"] = "",
    ["Image"] = "107267326632329",
    ["Color"] = Color3.fromRGB(135, 206, 250),
    ["Tab Width"] = 120,
    ["Theme"] = _G.ThemeID or nil
}

local FlurioreGui = FlurioreFixLib:MakeGui(GuiSettings)

local greak09plantbrainrot = "Blue X Hub | Plant Vs Brainrot V1.0.5"

if GuiSettings["NameHub"] ~= greak09plantbrainrot then
    LocalPlayer:Kick("Script Name Has Been Modified")
end

_G.AutoRejoin = true

spawn(function()
	    while wait() do
	        if _G.AutoRejoin then
	                getgenv().rejoin = game:GetService("CoreGui").RobloxPromptGui.promptOverlay.ChildAdded:Connect(function(child)
                        if child.Name == 'ErrorPrompt' and child:FindFirstChild('MessageArea') and child.MessageArea:FindFirstChild("ErrorFrame") then
                            game:GetService("TeleportService"):Teleport(game.PlaceId)
                        end
                     end)
	            end
	        end
	    end)
	    
local foldername = "Config Blue X Hub"
local filename = "PlantVsBrainrot_" .. game.Players.LocalPlayer.Name .. "_Config.json"
local filepath = foldername .. "/" .. filename

_G.Settings = _G.Settings or {
    DelayEquip = 5;
    BestBrainrot = false;
    Attack = false;
    DelayMoney = 5;
    Money = false;
    SubmitBrainrot = false;
    IgnoreBSell = {};
    IgnoreMSell = {};
    SellDelay = 5;
    SellAllBrainrot = false;
    BrainrotFavor = {};
    AutoFavorite = false;
    BrainrotUnFavor = {};
    AutoUnFavorite = false;
    DelayGift = "0.2";
    BrainrotGift = {};
    MutationBGift = {};
    AutoGiftBrainrot = false;
    PlantGift = {};
    AutoGiftPlant = false;
    UseWaterBucket = false;
    SelectSeed = {};
    SeedBuy = false;
    SelectGear = {};
    GearBuy = false;
}

function SaveSettings()
    local HttpService = game:GetService("HttpService")
    local json = HttpService:JSONEncode(_G.Settings)

    if writefile then
        if not isfolder(foldername) then
            makefolder(foldername)
        end
        writefile(filepath, json)
    end
end

function loadSettings()
    local HttpService = game:GetService("HttpService")

    if isfile(filepath) then
        local success, data = pcall(function()
            return HttpService:JSONDecode(readfile(filepath))
        end)

        if success and type(data) == "table" then
            for k, v in pairs(data) do
                _G.Settings[k] = v
            end
        end
    end
end

loadSettings()

FlurioreFixLib:MakeNotify({
	["Title"] = "Blue X Hub",
	["Description"] = "Notification",
	["Color"] = Color3.fromRGB(135, 206, 250),
	["Content"] = "Wait Load Script",
	["Time"] = 1,
	["Delay"] = 10
})

local Home = FlurioreGui:CreateTab({
	["Name"] = "Home",
	["Icon"] = "rbxassetid://7733960981"
})

local Discord = Home:AddSection("Discord")

Discord:AddButton({
	["Title"] = "Copy Link Discord",
	["Content"] = "discord.gg/yqaMxpYJy8",
	["Icon"] = "rbxassetid://16932740082",
	["Callback"] = function()
	    setclipboard("https://discord.gg/yqaMxpYJy8")
    end
})

local Update = Home:AddSection("New Update Script")

local function CreateUpdateUI()
    if player.PlayerGui:FindFirstChild("UpdateGui") then return end

    local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
    gui.Name = "UpdateGui"
    gui.ResetOnSpawn = false

    local mainFrame = Instance.new("Frame", gui)
    mainFrame.Size = UDim2.new(0, 500, 0, 350)
    mainFrame.Position = UDim2.new(0.5, -250, 0.5, -175)
    mainFrame.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    mainFrame.BorderSizePixel = 0
    mainFrame.BackgroundTransparency = 0
    mainFrame.Name = "MainFrame"
    mainFrame.Active = true
    mainFrame.Draggable = true

    local corner = Instance.new("UICorner", mainFrame)
    corner.CornerRadius = UDim.new(0, 20)

    local gradient = Instance.new("UIGradient", mainFrame)
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(70, 70, 70)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 120, 120))
    }

    local titleLabel = Instance.new("TextLabel", mainFrame)
    titleLabel.Size = UDim2.new(1, 0, 0, 60)
    titleLabel.Position = UDim2.new(0, 0, 0, 10)
    titleLabel.Text = "Update V1.0.5"
    titleLabel.Font = Enum.Font.FredokaOne
    titleLabel.TextSize = 40
    titleLabel.TextColor3 = Color3.fromRGB(230, 230, 230)
    titleLabel.BackgroundTransparency = 1
    titleLabel.TextStrokeTransparency = 0.6
    titleLabel.TextYAlignment = Enum.TextYAlignment.Top
    titleLabel.TextWrapped = true

    local subtitleLabel = Instance.new("TextLabel", mainFrame)
    subtitleLabel.Size = UDim2.new(1, -60, 0, 240)
    subtitleLabel.Position = UDim2.new(0, 30, 0, 75)
    subtitleLabel.Text =
        "[ + ] Add Gift Brainrot And Plant To Player (Tab Auto)"
    subtitleLabel.Font = Enum.Font.Nunito
    subtitleLabel.TextSize = 20
    subtitleLabel.TextColor3 = Color3.fromRGB(210, 210, 210)
    subtitleLabel.BackgroundTransparency = 1
    subtitleLabel.TextWrapped = true
    subtitleLabel.TextXAlignment = Enum.TextXAlignment.Center
    subtitleLabel.TextYAlignment = Enum.TextYAlignment.Top
end

local function DestroyUpdateUI()
    local gui = player:FindFirstChild("PlayerGui"):FindFirstChild("UpdateGui")
    if gui then
        gui:Destroy()
    end
end

Update:AddToggle({
    ["Title"] = "Update Script",
    ["Content"] = "",
    ["Default"] = false,
    ["Callback"] = function(value)
        _G.UpdateScript = value
        if value then
            CreateUpdateUI()
        else
            DestroyUpdateUI()
        end
    end
})

local M = FlurioreGui:CreateTab({
	["Name"] = "Farm",
	["Icon"] = "rbxassetid://7733960981"
})

local Equip = M:AddSection("Equip Brainrot")

Equip:AddSlider({
	["Title"] = "Delay Equip Brainrot (s)",
	["Content"] = "",
	["Min"] = 1,
	["Max"] = 60,
	["Increment"] = 1,
	["Default"] = _G.Settings.DelayEquip,
	["Callback"] = function(value) 
		_G.DelayEquip = value
		_G.Settings.DelayEquip = value
		SaveSettings()
	end
})

Equip:AddToggle({
    ["Title"] = "Auto Equip Best Brainrot",
    ["Content"] = "",
    ["Default"] = _G.Settings.BestBrainrot,
    ["Callback"] = function(value)
        _G.EquipBestBrainrot = value
        _G.Settings.BestBrainrot = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(_G.DelayEquip) do
        pcall(function()
            if _G.EquipBestBrainrot then
                game:GetService("ReplicatedStorage").Remotes.EquipBestBrainrots:FireServer()
            end
        end)
    end
end)

local Farm = M:AddSection("Farm")

Farm:AddToggle({
    ["Title"] = "Auto Attack Brainrot",
    ["Content"] = "",
    ["Default"] = _G.Settings.Attack,
    ["Callback"] = function(value)
        _G.AttackBrainrot = value
        _G.Settings.Attack = value
        SaveSettings()
    end
})

local rs = game:GetService("ReplicatedStorage")
local attacksRemote = rs.Remotes.AttacksServer.WeaponAttack

function EquipBat()
    if backpack and backpack:FindFirstChild("Basic Bat") then
        backpack["Basic Bat"].Parent = character
    end
end

function GetMyPlot()
    for i = 1, 6 do
        local plot = workspace.Plots:FindFirstChild(tostring(i))
        if plot then
            local sign = plot:FindFirstChild("PlayerSign")
            local gui = sign and sign:FindFirstChild("BillboardGui")
            local label = gui and gui:FindFirstChild("TextLabel")
            
            if label and label.Text == lp.Name then
                return plot
            end
        end
    end
end

spawn(function()
    while task.wait(0.1) do
        pcall(function()
            if _G.AttackBrainrot then
                local myPlot = GetMyPlot()
                if not myPlot then return end

                local sign = myPlot:FindFirstChild("PlayerSign")
                if not sign then return end

                for _, brainrot in ipairs(workspace.ScriptedMap.Brainrots:GetChildren()) do
                    if brainrot:IsA("Model") and brainrot:FindFirstChild("BrainrotHitbox") then
                        local dist = (brainrot.BrainrotHitbox.Position - sign.Position).Magnitude
                        if dist <= 100 then
                            while brainrot.Parent and _G.AttackBrainrot do
                                TP(brainrot.BrainrotHitbox.CFrame * CFrame.new(0, 3, 0))

                                EquipBat()

                                local args = {
                                    [1] = {
                                        [1] = brainrot.Name
                                    }
                                }
                                attacksRemote:FireServer(unpack(args))

                                task.wait(0.1)
                            end
                        end
                    end
                end
            end
        end)
    end
end)

Farm:AddSlider({
	["Title"] = "Delay Collect Money (s)",
	["Content"] = "",
	["Min"] = 1,
	["Max"] = 60,
	["Increment"] = 1,
	["Default"] = _G.Settings.DelayMoney,
	["Callback"] = function(value) 
		_G.DelayMoney = value
		_G.Settings.DelayMoney = value
		SaveSettings()
	end
})

Farm:AddToggle({
    ["Title"] = "Auto Collect Money",
    ["Content"] = "",
    ["Default"] = _G.Settings.Money,
    ["Callback"] = function(value)
        _G.CollectMoney = value
        _G.Settings.Money = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(_G.DelayMoney) do
        pcall(function()
            if _G.CollectMoney then
                local myPlot = GetMyPlot()
                if not myPlot then return end

                local plantsFolder = myPlot:FindFirstChild("Plants")
                if not plantsFolder then return end

                local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                if not hrp then return end

                local originalCFrame = hrp.CFrame

                for _, plant in ipairs(plantsFolder:GetChildren()) do
                    local money = plant:GetAttribute("Money")
                    if money and money > 0 then
                        local root = plant:FindFirstChildWhichIsA("BasePart") or plant:FindFirstChild("Main") or plant:FindFirstChild("RootPart")
                        if root then
                            TP(root.CFrame * CFrame.new(0, 3, 0))
                            task.wait(0.2)
                        end
                    end
                end

                TP(originalCFrame)
            end
        end)
    end
end)

local Prison = M:AddSection("Prison Event")

Prison:AddToggle({
    ["Title"] = "Auto Submit Brainrot To Prison",
    ["Content"] = "",
    ["Default"] = _G.Settings.SubmitBrainrot,
    ["Callback"] = function(value)
        _G.SubmitPrison = value
        _G.Settings.SubmitBrainrot = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(1) do
        pcall(function()
            if _G.SubmitPrison then
                local wantedGui = lp:FindFirstChild("PlayerGui")
                    and lp.PlayerGui:FindFirstChild("Main")
                    and lp.PlayerGui.Main:FindFirstChild("WantedPosterGui")
                    and lp.PlayerGui.Main.WantedPosterGui:FindFirstChild("Frame")
                    and lp.PlayerGui.Main.WantedPosterGui.Frame:FindFirstChild("Main")
                    and lp.PlayerGui.Main.WantedPosterGui.Frame.Main:FindFirstChild("WantedItem")
                    and lp.PlayerGui.Main.WantedPosterGui.Frame.Main.WantedItem:FindFirstChild("WantedItem_Title")

                if not wantedGui then return end

                local wantedName = wantedGui.Text
                if wantedName == "" or not wantedName then return end

                local function findWantedItem()
                    for _, tool in ipairs(backpack:GetChildren()) do
                        if tool:IsA("Tool") and string.find(tool.Name, wantedName) then
                            return tool
                        end
                    end
                    for _, tool in ipairs(character:GetChildren()) do
                        if tool:IsA("Tool") and string.find(tool.Name, wantedName) then
                            return tool
                        end
                    end
                    return nil
                end

                local wantedItem = findWantedItem()
                if wantedItem then
                    if backpack:FindFirstChild(wantedItem.Name) then
                        lp.Character.Humanoid:EquipTool(wantedItem)
                        task.wait(0.3)
                    end
                    
                    TP(CFrame.new(-185.43629455566406, 12.482851028442383, 1005.6113891601562) * CFrame.new(0, 3, 0))

                    local args = {
                        [1] = "TurnIn"
                    }

                    game:GetService("ReplicatedStorage").Remotes.Events.Prison.Interact:FireServer(unpack(args))
                end
            end
        end)
    end
end)

local Sell = M:AddSection("Sell Brainrot")

local brainrots = {}
for _, obj in ipairs(game:GetService("ReplicatedStorage").Assets:WaitForChild("Brainrots"):GetChildren()) do
    table.insert(brainrots, obj.Name)
end
table.sort(brainrots)

Sell:AddSlider({
	["Title"] = "Delay Sell Brainrot (s)",
	["Content"] = "",
	["Min"] = 1,
	["Max"] = 20,
	["Increment"] = 1,
	["Default"] = _G.Settings.SellDelay,
	["Callback"] = function(value) 
		_G.DelaySell = value
		_G.Settings.SellDelay = value
		SaveSettings()
	end
})

Sell:AddToggle({
    ["Title"] = "Auto Sell All Brainrot",
    ["Content"] = "",
    ["Default"] = _G.Settings.SellAllBrainrot,
    ["Callback"] = function(value)
        _G.SellBrainrot = value
        _G.Settings.SellAllBrainrot = value
        SaveSettings()
    end
})

local backpackGui = lp.PlayerGui:WaitForChild("BackpackGui")
local hotbar = backpackGui.Backpack.Hotbar
local inventory = backpackGui.Backpack.Inventory.ScrollingFrame.UIGridFrame

spawn(function()
    while task.wait(_G.DelaySell) do
        pcall(function()
            if _G.SellBrainrot then
                game:GetService("ReplicatedStorage").Remotes.ItemSell:FireServer()
            end
        end)
    end
end)
            
Sell:AddButton({
	["Title"] = "Sell All Brainrot",
	["Content"] = "",
	["Icon"] = "rbxassetid://16932740082",
	["Callback"] = function()
	    game:GetService("ReplicatedStorage").Remotes.ItemSell:FireServer()
    end
})

local A = FlurioreGui:CreateTab({
	["Name"] = "Auto",
	["Icon"] = "rbxassetid://7733960981"
})

local Favor = A:AddSection("Favorite & Unfavorite")

Favor:AddText({
	["Title"] = "Favorite Brainrot"
})

Favor:AddDropdown({
    ["Title"] = "Brainrot Favorite",
    ["Content"] = "",
    ["Multi"] = true,
    ["Options"] = brainrots,
    ["Default"] = _G.Settings.BrainrotFavor,
    ["Callback"] = function(value)
        _G.BrainrotFavorite = value
        _G.Settings.BrainrotFavor = value
        SaveSettings()
    end
})

Favor:AddToggle({
    ["Title"] = "Auto Favorite Brainrot",
    ["Content"] = "",
    ["Default"] = _G.Settings.AutoFavorite,
    ["Callback"] = function(value)
        _G.AutoFavorite = value
        _G.Settings.AutoFavorite = value
        SaveSettings()
    end
})
                
spawn(function()
    while task.wait(0.1) do
        pcall(function()
            if _G.AutoFavorite and _G.BrainrotFavorite and typeof(_G.BrainrotFavorite) == "table" then
                for _, favName in ipairs(_G.BrainrotFavorite) do
                    for _, tool in ipairs(backpack:GetChildren()) do
                        if string.find(tool.Name, favName) then
                            local itemId = tool:GetAttribute("ID")
                            if itemId then
                                local slots = {}
                                for _, v in ipairs(hotbar:GetChildren()) do
                                    table.insert(slots, v)
                                end
                                for _, v in ipairs(inventory:GetChildren()) do
                                    table.insert(slots, v)
                                end

                                for _, slot in ipairs(slots) do
                                    if slot:GetAttribute("ID") == itemId then
                                        if not slot:FindFirstChild("HeartIcon") then
                                            local args = {[1] = itemId}
                                            rs.Remotes.FavoriteItem:FireServer(unpack(args))
                                            task.wait(0.6)
                                        end
                                    end
                                end
                            end
                        end
                    end

                    for _, tool in ipairs(character:GetChildren()) do
                        if tool:IsA("Tool") and string.find(tool.Name, favName) then
                            local itemId = tool:GetAttribute("ID")
                            if itemId then
                                local slots = {}
                                for _, v in ipairs(hotbar:GetChildren()) do
                                    table.insert(slots, v)
                                end
                                for _, v in ipairs(inventory:GetChildren()) do
                                    table.insert(slots, v)
                                end

                                for _, slot in ipairs(slots) do
                                    if slot:GetAttribute("ID") == itemId then
                                        if not slot:FindFirstChild("HeartIcon") then
                                            local args = {[1] = itemId}
                                            rs.Remotes.FavoriteItem:FireServer(unpack(args))
                                            task.wait(0.6)
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end)
    end
end)

Favor:AddText({
	["Title"] = "Unfavorite Brainrot"
})

Favor:AddDropdown({
    ["Title"] = "Brainrot Unfavorite",
    ["Content"] = "",
    ["Multi"] = true,
    ["Options"] = brainrots,
    ["Default"] = _G.Settings.BrainrotUnFavor,
    ["Callback"] = function(value)
        _G.BrainrotUnFavorite = value
        _G.Settings.BrainrotUnFavor = value
        SaveSettings()
    end
})

Favor:AddToggle({
    ["Title"] = "Auto Unfavorite Brainrot",
    ["Content"] = "",
    ["Default"] = _G.Settings.AutoUnFavorite,
    ["Callback"] = function(value)
        _G.AutoUnFavorite = value
        _G.Settings.AutoUnFavorite = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(0.1) do
        pcall(function()
            if _G.AutoUnFavorite and _G.BrainrotUnFavorite and typeof(_G.BrainrotUnFavorite) == "table" then               
                for _, favName in ipairs(_G.BrainrotUnFavorite) do
                    for _, tool in ipairs(backpack:GetChildren()) do
                        if string.find(tool.Name, favName) then
                            local itemId = tool:GetAttribute("ID")
                            if itemId then
                                local slots = {}
                                for _, v in ipairs(hotbar:GetChildren()) do
                                    table.insert(slots, v)
                                end
                                for _, v in ipairs(inventory:GetChildren()) do
                                    table.insert(slots, v)
                                end

                                for _, slot in ipairs(slots) do
                                    if slot:GetAttribute("ID") == itemId then
                                        if slot:FindFirstChild("HeartIcon") then
                                            local args = {[1] = itemId}
                                            rs.Remotes.FavoriteItem:FireServer(unpack(args))
                                            task.wait(0.6)
                                        end
                                    end
                                end
                            end
                        end
                    end

                    for _, tool in ipairs(character:GetChildren()) do
                        if tool:IsA("Tool") and string.find(tool.Name, favName) then
                            local itemId = tool:GetAttribute("ID")
                            if itemId then
                                local slots = {}
                                for _, v in ipairs(hotbar:GetChildren()) do
                                    table.insert(slots, v)
                                end
                                for _, v in ipairs(inventory:GetChildren()) do
                                    table.insert(slots, v)
                                end

                                for _, slot in ipairs(slots) do
                                    if slot:GetAttribute("ID") == itemId then
                                        if slot:FindFirstChild("HeartIcon") then
                                            local args = {[1] = itemId}
                                            rs.Remotes.FavoriteItem:FireServer(unpack(args))
                                            task.wait(0.6)
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end)
    end
end)

local Gift = A:AddSection("Gift Brainrot & Plant")

Gift:AddText({
	["Title"] = "Setting Gift"
})

local Playerslist = {}
for i,v in pairs(game:GetService("Players"):GetChildren()) do
    table.insert(Playerslist,v.Name)
end

Gift:AddDropdown({
    ["Title"] = "Select Player Gift",
    ["Content"] = "",
    ["Multi"] = false,
    ["Options"] = Playerslist,
    ["Default"] = Playerslist[2],
    ["Callback"] = function(value)
        _G.PlayerGift = value
    end
})

Gift:AddInput({
	["Title"] = "Delay Gift (s)",
	["Content"] = "",
	["Default"] = _G.Settings.DelayGift,
	["Callback"] = function(value) 
		_G.DelayGift = value
		_G.Settings.DelayGift = value
		SaveSettings()
	end
})

Gift:AddText({
	["Title"] = "Gift Brainrot"
})

Gift:AddDropdown({
    ["Title"] = "Select Brainrot Gift",
    ["Content"] = "",
    ["Multi"] = true,
    ["Options"] = brainrots,
    ["Default"] = _G.Settings.BrainrotGift,
    ["Callback"] = function(value)
        _G.BrainrotGift = value
        _G.Settings.BrainrotGift = value
        SaveSettings()
    end
})

Gift:AddToggle({
    ["Title"] = "Auto Gift Brainrot To Player",
    ["Content"] = "",
    ["Default"] = _G.Settings.AutoGiftBrainrot,
    ["Callback"] = function(value)
        _G.AutoGiftBrainrot = value
        _G.Settings.AutoGiftBrainrot = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait() do
        pcall(function()
            if _G.AutoGiftBrainrot and typeof(_G.BrainrotGift) == "table" and _G.PlayerGift then
                for _, brainrotName in ipairs(_G.BrainrotGift) do
                    local function findBrainrot()
                        for _, tool in ipairs(backpack:GetChildren()) do
                            if tool:IsA("Tool") and string.find(tool.Name, brainrotName) then
                                return tool
                            end
                        end
                        for _, tool in ipairs(character:GetChildren()) do
                            if tool:IsA("Tool") and string.find(tool.Name, brainrotName) then
                                return tool
                            end
                        end
                        return nil
                    end

                    local found = findBrainrot()
                    if found then
                        if backpack:FindFirstChild(found.Name) then
                            lp.Character.Humanoid:EquipTool(found)
                            task.wait(0.3)
                        end

                        local itemEquipped = lp.Character:FindFirstChildWhichIsA("Tool")
                        if itemEquipped then
                            local args = {
                                [1] = {
                                    ["Item"] = itemEquipped,
                                    ["ToGift"] = _G.PlayerGift
                                }
                            }

                            game:GetService("ReplicatedStorage").Remotes.GiftItem:FireServer(unpack(args))                            
                        end
                    end
                    task.wait(tonumber(_G.DelayGift))
                end
            end
        end)
    end
end)

Gift:AddText({
	["Title"] = "Gift Plant"
})

local plantlist = {}
local PlantsFolder = rs.Assets.Animations:WaitForChild("Plants")

for _, obj in ipairs(PlantsFolder:GetChildren()) do
    table.insert(plantlist, obj.Name)
end

table.sort(plantlist)

Gift:AddDropdown({
    ["Title"] = "Select Plant Gift",
    ["Content"] = "",
    ["Multi"] = true,
    ["Options"] = plantlist,
    ["Default"] = _G.Settings.PlantGift,
    ["Callback"] = function(value)
        _G.PlantGift = value
        _G.Settings.PlantGift = value
        SaveSettings()
    end
})

Gift:AddToggle({
    ["Title"] = "Auto Gift Plant To Player",
    ["Content"] = "",
    ["Default"] = _G.Settings.AutoGiftPlant,
    ["Callback"] = function(value)
        _G.AutoGiftPlant = value
        _G.Settings.AutoGiftPlant = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait() do
        pcall(function()
            if _G.AutoGiftPlant and typeof(_G.PlantGift) == "table" and _G.PlayerGift then
                for _, plantName in ipairs(_G.PlantGift) do
                    local function findPlant()
                        for _, tool in ipairs(backpack:GetChildren()) do
                            if tool:IsA("Tool") and string.find(tool.Name, plantName) and not string.find(tool.Name:lower(), "seed") then
                                return tool
                            end
                        end
                        for _, tool in ipairs(character:GetChildren()) do
                            if tool:IsA("Tool") and string.find(tool.Name, plantName) and not string.find(tool.Name:lower(), "seed") then
                                return tool
                            end
                        end
                        return nil
                    end

                    local found = findPlant()
                    if found then
                        if backpack:FindFirstChild(found.Name) then
                            lp.Character.Humanoid:EquipTool(found)
                            task.wait(0.3)
                        end

                        local itemEquipped = lp.Character:FindFirstChildWhichIsA("Tool")
                        if itemEquipped then
                            local args = {
                                [1] = {
                                    ["Item"] = itemEquipped,
                                    ["ToGift"] = _G.PlayerGift
                                }
                            }
                            game:GetService("ReplicatedStorage").Remotes.GiftItem:FireServer(unpack(args))
                        end
                    end
                    task.wait(tonumber(_G.DelayGift))
                end
            end
        end)
    end
end)

local Water = A:AddSection("Use Water Bucket")

local SavedWaterPos = nil

Water:AddButton({
    ["Title"] = "Save Pos Use Water Bucket",
    ["Content"] = "",
    ["Icon"] = "rbxassetid://16932740082",
    ["Callback"] = function()        
        if char and char:FindFirstChild("HumanoidRootPart") then            
            SavedWaterPos = char.HumanoidRootPart.CFrame.Position
        end
        local Notify = FlurioreFixLib:MakeNotify({
	        ["Title"] = "Blue X Hub",
	        ["Description"] = "Notification",
	        ["Color"] = Color3.fromRGB(135, 206, 250),
	        ["Content"] = "Save Pos Use Water Bucket Success",
	        ["Time"] = 1,
	        ["Delay"] = 10
        })
    end
})

Water:AddToggle({
    ["Title"] = "Auto Use Water Bucket",
    ["Content"] = "",
    ["Default"] = _G.Settings.UseWaterBucket,
    ["Callback"] = function(value)
        _G.UseWater = value
        _G.Settings.UseWaterBucket = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(1) do
        pcall(function()
            if _G.UseWater and SavedWaterPos then
                local bucket = nil
                for _, tool in ipairs(backpack:GetChildren()) do
                    if string.find(tool.Name, "Water Bucket") then
                        bucket = tool
                        player.Character.Humanoid:EquipTool(tool)
                        break
                    end
                end

                if not bucket then
                    for _, tool in ipairs(character:GetChildren()) do
                        if tool:IsA("Tool") and string.find(tool.Name, "Water Bucket") then
                            bucket = tool
                            break
                        end
                    end
                end

                if bucket and character:FindFirstChild(bucket.Name) then
                    local args = {
                        [1] = {
                            ["Toggle"] = true,
                            ["Tool"] = character:FindFirstChild(bucket.Name),
                            ["Pos"] = Vector3.new(SavedWaterPos)
                        }
                    }
                    rs.Remotes.UseItem:FireServer(unpack(args))
                    task.wait(0.2)
                end
            end
        end)
    end
end)

local Sh = FlurioreGui:CreateTab({
	["Name"] = "Shop",
	["Icon"] = "rbxassetid://7733960981"
})

local Shop = Sh:AddSection("Shop")

local seedlist = {
    "Cactus Seed", "Strawberry Seed", "Pumpkin Seed", "Sunflower Seed",
    "Dragon Fruit Seed", "Eggplant Seed", "Watermelon Seed", "Grape Seed",
    "Cocotank Seed", "Carnivorous Seed", "Mr Carrot Seed", "Tomatrio Seed",
    "Shroombino Seed", "Mango Seed"
}

Shop:AddDropdown({
    ["Title"] = "Select Seed Buy",
    ["Content"] = "",
    ["Multi"] = true,
    ["Options"] = seedlist,
    ["Default"] = _G.Settings.SelectSeed,
    ["Callback"] = function(value)
        _G.ChonSeedBuy = value
        _G.Settings.SelectSeed = value
        SaveSettings()
    end
})

Shop:AddToggle({
    ["Title"] = "Auto Buy Seed",
    ["Content"] = "",
    ["Default"] = _G.Settings.SeedBuy,
    ["Callback"] = function(value)
        _G.BuySeed = value
        _G.Settings.SeedBuy = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(0.2) do
        if _G.BuySeed and _G.ChonSeedBuy and typeof(_G.ChonSeedBuy) == "table" then
            pcall(function()
                for _, seedName in ipairs(_G.ChonSeedBuy) do
                    local stockLabel = game:GetService("Players").LocalPlayer.PlayerGui.Main.Seeds.Frame.ScrollingFrame:FindFirstChild(seedName)
                    if stockLabel and stockLabel:FindFirstChild("Stock") then
                        if not string.find(stockLabel.Stock.Text, "x0 in stock") then
                            local args = {
                                [1] = seedName,
                                [2] = true
                            }
                            game:GetService("ReplicatedStorage").Remotes.BuyItem:FireServer(unpack(args))
                            task.wait(0.1)
                        end
                    end
                end
            end)
        end
    end
end)

local gearlist = {
    "Water Bucket", "Frost Grenade", "Banana Gun", "Frost Blower",
    "Carrot Launcher"
}

Shop:AddDropdown({
    ["Title"] = "Select Gear Buy",
    ["Content"] = "",
    ["Multi"] = true,
    ["Options"] = gearlist,
    ["Default"] = _G.Settings.SelectGear,
    ["Callback"] = function(value)
        _G.SelectGear = value
        _G.Settings.SelectGear = value
        SaveSettings()
    end
})

Shop:AddToggle({
    ["Title"] = "Auto Buy Gear",
    ["Content"] = "",
    ["Default"] = _G.Settings.GearBuy,
    ["Callback"] = function(value)
        _G.BuyGear = value
        _G.Settings.GearBuy = value
        SaveSettings()
    end
})

spawn(function()
    while task.wait(0.2) do
        if _G.BuyGear and _G.SelectGear and typeof(_G.SelectGear) == "table" then
            pcall(function()
                for _, gearName in ipairs(_G.SelectGear) do
                    local stockLabel = game:GetService("Players").LocalPlayer.PlayerGui.Main.Gears.Frame.ScrollingFrame:FindFirstChild(gearName)
                    if stockLabel and stockLabel:FindFirstChild("Stock") then
                        if not string.find(stockLabel.Stock.Text, "x0 in stock") then
                            local args = {
                                [1] = gearName,
                                [2] = true  
                            }
                            game:GetService("ReplicatedStorage").Remotes.BuyGear:FireServer(unpack(args))
                            task.wait(0.1)
                        end
                    end
                end
            end)
        end
    end
end)

local Misc = FlurioreGui:CreateTab({
	["Name"] = "Misc",
	["Icon"] = "rbxassetid://7733960981"
})

local Vis = Misc:AddSection("Dupe Item (Visual)")

Vis:AddButton({
	["Title"] = "Click To Dupe Equipped Item",
	["Content"] = "",
	["Icon"] = "rbxassetid://16932740082",
	["Callback"] = function()
	    local tool = char:FindFirstChildOfClass("Tool")
	    if tool then
	        local dupe = tool:Clone()
            dupe.Name = tool.Name .. " (Dupe)"
            dupe.Parent = backpack
        end
    end
})

FlurioreFixLib:MakeNotify({
	["Title"] = "Blue X Hub",
	["Description"] = "Notification",
	["Color"] = Color3.fromRGB(135, 206, 250),
	["Content"] = "Done Load Script",
	["Time"] = 1,
	["Delay"] = 10
})

local playerName = player.Name
local displayName = player.DisplayName

local executorName = "Unknown"
if identifyexecutor then
    executorName = identifyexecutor()
end

function sendWebhookUseS(title, message)
    local HttpService = game:GetService("HttpService")

    local data = {
        ["content"] = "Username: "..playerName.."\nDisplayName: "..displayName.."\nExecutors: "..executorName,
        ["embeds"] = {
            {
                ["title"] = title,
                ["description"] = message,
                ["color"] = 65280,
                ["footer"] = {
                    ["text"] = "Blue X Hub"
                },
                ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }
        }
    }

    local success, err = pcall(function()
        local encoded = HttpService:JSONEncode(data)
        local requestFunc = http_request or request or HttpPost or syn.request
        if requestFunc then
            requestFunc({
                Url = "https://discord.com/api/webhooks/1424042524305129552/IajRVZjY-bTx-9j3CvSAOJ6YW4higzxtuSGDxKRUm7inrMTjdNkyopVExAQeG_6ZYXNH",
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = encoded
            })
        else
            warn("Exploit không hỗ trợ gửi HTTP Request")
        end
    end)

    if not success then
        warn("Lỗi gửi webhook: ", err)
    end
end

if not stopsendWebhook then   
    sendWebhookUseS("Notification", "Main Plant Vs Brainrot")
    wait(1)
    stopsendWebhook = true
end